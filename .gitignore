# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Credentials — never commit
kaggle.json
access_token
*.pem

# Model artifacts / training outputs (modeling phase)
*.pt
*.bin
*.safetensors
*.ckpt
*.h5
checkpoints/
runs/
wandb/
mlruns/
outputs/

# Misc
*.log
.cache/

# Dataset — kept local only, never pushed (published separately on Kaggle)
data/

# Archived scratch: intermediate per-record dumps + superseded docs (kept local)
archive/

# Per-record tagged dumps the generator may emit into report/ (scratch, never push)
report/*.tagged.txt
